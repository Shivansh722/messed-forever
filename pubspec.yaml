name: mess
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  hidden_drawer_menu: 3.0.1
  lottie: ^2.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  
  # Your other regular dependencies here

flutter:
  uses-material-design: true
  assets:
    - lib/assets/madarchod.png

  # Additional configuration for fonts can be added here if needed.
